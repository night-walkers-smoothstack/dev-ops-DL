apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: aline-underwriter
  name: aline-underwriter
spec:
  selector:
    matchLabels:
      app: aline-underwriter
  replicas: 1
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: aline-underwriter
    spec:
      containers:
        - name: aline-underwriter
          image: underwriter
          imagePullPolicy: Never
          resources:
            limits:
              memory: "128Mi"
              cpu: "500m"
          ports:
          - containerPort: 8071
          env:
          - name: APP_PORT
            value: "8071"
          envFrom:
          - configMapRef:
              name: db-config
          - configMapRef:
              name: portal-config
          - secretRef:
              name: db-secrets
          - secretRef:
              name: ms-secrets
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: aline-underwriter
spec:
  selector:
    app: aline-underwriter
  ports:
  - port: 8071
    targetPort: 8071

