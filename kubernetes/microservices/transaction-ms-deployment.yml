apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: aline-transaction
  name: aline-transaction
spec:
  selector:
    matchLabels:
      app: aline-transaction
  replicas: 1
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: aline-transaction
    spec:
      containers:
        - name: aline-transaction
          image: transaction
          imagePullPolicy: Never
          resources:
            limits:
              memory: "128Mi"
              cpu: "500m"
          ports:
          - containerPort: 8073
          env:
          - name: APP_PORT
            value: "8073"
          envFrom:
          - configMapRef:
              name: db-config
          - configMapRef:
              name: portal-config
          - secretRef:
              name: db-secrets
          - secretRef:
              name: ms-secrets
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: aline-transaction
spec:
  selector:
    app: aline-transaction
  ports:
  - port: 8073

  